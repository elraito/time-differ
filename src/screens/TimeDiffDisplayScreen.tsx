import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

export function TimeDiffDisplayScreen(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>TimeDiffDisplayScreen</Text>
    </SafeAreaView>
  );
}
