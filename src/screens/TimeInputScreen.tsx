import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native';

export function TimeInputScreen(): JSX.Element {
  return (
    <SafeAreaView>
      <Text>TimeInputScreen</Text>
    </SafeAreaView>
  );
}
